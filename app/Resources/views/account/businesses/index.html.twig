{% extends 'admin.html.twig' %}

{% block body %}

<div class="nav-pad">
{{ form_start(form) }}
{{ form_widget(form) }}
  <input type="submit" value="Create"
       class="btn btn-default pull-right" />
{{ form_end(form) }}
</div>

{% endblock %}
